{
  "AppSettings": {
    "IsProduction": false,
    "PlantID": "", // ALP, BUT, FTW, POR, AND
    "PollingIncrement": 10, // seconds
    "PseudoPlantID": "SANDBOX",
    // Set the number of records to process while testing.
    // Only works if IsProduction flag is set to 'false'
    // 0 = process all records
    "NumberOfRecordsToTest": 0
  }
}